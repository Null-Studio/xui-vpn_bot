# 🚀 ربات فروش وی‌پی‌ان NukeNet

این فایل راهنمای فارسی پروژه است. برای بازگشت به صفحه اصلی [اینجا کلیک کنید](README.md).

## ✨ قابلیت‌ها
* **اتصال به 3x-ui:** ساخت خودکار اکانت بعد از پرداخت.
* **درگاه کریپتو:** پشتیبانی از تتر و ترون.
* **سیستم زیرمجموعه‌گیری:** بنر اختصاصی برای هر کاربر.
* **تست رایگان:** امکان تست قبل از خرید.

## 🛠 راهنمای نصب
۱. پروژه رو دانلود کنید.
۲. کتابخانه‌ها رو نصب کنید: `pip install -r requirements.txt`
۳. فایل `.env.example` رو به `.env` تغییر نام بدید و اطلاعاتتون رو توش بذارید.
۴. ربات رو با دستور `python vpnbot.py` اجرا کنید.

---
**نکته امنیتی:** هرگز اطلاعات فایل `.env` رو در اختیار کسی قرار ندید.
